spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  output:
    ansi:
      enabled: ALWAYS # 로그 색깔 설정

management:
  endpoints:
    web:
      exposure:
        include: "*" # actuator 설정

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

datago:
  auth-key: 123

my:
  opensearch:
    uris: localhost:9200

